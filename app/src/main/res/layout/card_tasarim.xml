<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
   >

    <androidx.cardview.widget.CardView
        android:id="@+id/filmCard"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_margin="5dp"

        app:cardCornerRadius="5dp"
        app:cardElevation="5dp"
        >

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="240dp"
            android:background="@drawable/film">

            <ImageView
                android:id="@+id/cardResim"
                android:layout_width="100dp"
                android:layout_height="150dp"
                android:layout_centerHorizontal="true"
                app:srcCompat="@drawable/django" />

            <TextView
                android:id="@+id/cardBaslik"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_below="@id/cardResim"
                android:layout_centerHorizontal="true"
                android:text="Django"
                android:textSize="18sp"
                android:textStyle="bold" />

            <Button
                android:id="@+id/cardButon"
                android:layout_width="111dp"
                android:layout_height="wrap_content"
                android:layout_below="@id/cardBaslik"
                android:layout_centerHorizontal="true"
                android:backgroundTint="#81d4fa"
                android:text="İzle"
                tools:text="İzle" />
        </RelativeLayout>
    </androidx.cardview.widget.CardView>
</LinearLayout>